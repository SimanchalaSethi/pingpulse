apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: pingpulse
spec:
  selector:
    app: analytics-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP
